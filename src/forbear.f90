!< **forbear** project, Fortran (progress) B(e)ar environment.

module forbear
!< **forbear** project, Fortran (progress) B(e)ar environment.
use forbear_bar_object, only : bar_object
implicit none
private
public :: bar_object
endmodule forbear
